name: Repository Hygiene

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  hygiene:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Run verification script
      run: ./verify-complete.sh
